.job_template_drop:
  stage: drop
  image:
    name: https://gitlab.com/shortlink-org/dependency_proxy/containers/alpine/helm:3.4.1
    entrypoint: [""]
  script:
    - env
    - helm del -n $KUBE_NAMESPACE $RELEASE_NAME $HELM_ARG;
  retry: 2
  when: manual
